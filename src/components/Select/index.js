import React from 'react';

const index = (props) => {
    return (
        <select>
            <option value="">Prochaine lettre dans l'alphabet</option>
        </select>
    );
};

export default index;